<%= form_for [@user, item] do |i| %>
  <div class="form-group">
    <%= i.label :name %>
    <%= i.text_field :name, class: 'form-control', placeholder: "Enter item name" %>
  </div>
  <div class="form-group">
    <%= i.submit "Add", class: 'btn btn-success' %>
  </div>
<% end %>
